enum Status { PUBLIC, PRIVATE, REQUEST }

enum StatusJoin { APPROVED, PENDING, REJECTED, NOT_JOINED }

enum LearningDurationType { LIMIT, NOLIMIT }

enum FileType {
  pdf,
  docx,
  text,
  binary,
  unknown,
  potentially_text,
}
